{
  "name": "sahayata-board",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:server": "npm install --prefix server",
    "start": "npm start --prefix server",
    "dev": "npm run dev --prefix server"
  },
  "engines": {
    "node": ">=18"
  }
}
